[project]
name = "court-opinions-scripts"
version = "0.1.0"
description = "Scripts to download court opinions from CourtListener for RAG evaluation"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "tqdm>=4.66.0",
]

[dependency-groups]
dev = []
